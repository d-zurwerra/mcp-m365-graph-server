name: Build & Deploy MCP Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  AZURE_CONTAINER_APP: oskar-mcp-server
  AZURE_RESOURCE_GROUP: rg-oskar-mcp-env

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build & Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # TODO (Produktion): Deploy-Job aktivieren sobald Michael die App Registration
  # 'oskar-github-actions-deploy' als Contributor auf rg-oskar-mcp-env hinzugefügt hat.
  # Dann auch die App Registration von DEV-Tenant in den in2success Tenant umziehen.
  # Benötigte GitHub Secrets: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID
  #
  # deploy:
  #   name: Deploy to Azure Container Apps
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   permissions:
  #     id-token: write
  #     contents: read
  #   steps:
  #     - name: Azure Login (Workload Identity Federation)
  #       uses: azure/login@v2
  #       with:
  #         client-id: ${{ secrets.AZURE_CLIENT_ID }}
  #         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
  #         subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  #     - name: Deploy to Container Apps
  #       uses: azure/container-apps-deploy-action@v1
  #       with:
  #         resourceGroup: ${{ env.AZURE_RESOURCE_GROUP }}
  #         containerAppName: ${{ env.AZURE_CONTAINER_APP }}
  #         imageToDeploy: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  #         registryUrl: ${{ env.REGISTRY }}
  #         registryUsername: ${{ github.actor }}
  #         registryPassword: ${{ secrets.GITHUB_TOKEN }}
